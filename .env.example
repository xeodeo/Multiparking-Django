DB_NAME=multiparking
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306

# Email — Gmail SMTP (App Password). Si no se define, usa SendGrid como fallback.
GMAIL_USER=tucorreo@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
EMAIL_FROM=Multiparking <tucorreo@gmail.com>

# Email — SendGrid (fallback si GMAIL_APP_PASSWORD no está definido)
SENDGRID_API_KEY=